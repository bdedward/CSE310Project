#include<iostream>

using namespace std;

//typedef int key;
//Structs should be in the heap.h
struct ELEMENT {
	int key;
};

struct HEAP {
	int capacity;
	int size;
	struct ELEMENT *H;
};

Initialize(n){
	H
}

BuildHeap(heap, A, n){	
}

Insert(heap, flag, k){	
}

DeleteMin(heap, flag){
}

DecreaseKey(heap, flag, index, value){
}

printHeap(heap){
}
